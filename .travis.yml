language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g -d

after_script:
  - cd ./public
  - git init
  - git config user.name "黄荣君"
  - git config user.email "l1091462907@gmail.com"
  - git add .
  - git commit -m " Site updated"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog-source
env:
 global:
   - GH_REF: git@github.com:iCrazyTeam/iCrazyTeam.github.io.git